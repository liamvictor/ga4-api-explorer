Completed Since Last Checkpoint:
   * Full Command-Line Flag Support: run_report.py now accepts flags for property, report, date range (-sd, -ed), and output format (-o), allowing for non-interactive execution.
   * Cache Cleanup: Implemented an automatic cleanup for stale cache files.
   * Configurable Settings: CACHE_DURATION moved to settings.py.
   * New Reports: Added AI Traffic Acquisition, Top Pages, Channel Overview, Outbound Click Tracking, Cohort Retention, File Downloads, and Device Type (Historical & Snapshot).
   * Improved Filenaming: Output filenames now reflect the actual date range and property.
   * Documentation Sync: README.md and SETUP_GUIDE.md are fully updated with 24 active reports and setup instructions.
   * Property List Caching: Accelerated property selection with persistence.
   * Interactive "P" Action: Added "(P) Run same report on another property" to preserve report/date settings during comparison.
   * Multi-Month Trend Engine: Implemented 'Last 13 Months' and 'All Months' logic using the yearMonth dimension for longitudinal studies.
   * Aggressive UTM URL Cleaning: Implemented stripping of fragmenting parameters (msclkid, gclid, Marketo/HubSpot IDs) and re-aggregation of data in UTM reports.
   * Cohort Documentation: Added built-in "How to Read" guide to the Cohort Retention HTML report.

Next Tasks & Improvements:

   1. **Non-Interactive Date Presets:**
      * Add CLI flags like `--last-7-days`, `--last-30-days`, `--last-month`, and `--last-13-months` to avoid manual date entry in scripts.

   2. **"All Key Events" Performance Report:**
      * Create a report that tracks ALL events marked as "Key Events" (conversions) in GA4, not just 'generate_lead', broken down by source/medium.

   3. **On-Site Search Analysis:**
      * Create a report for 'view_search_results' to identify what users are looking for and where content gaps exist.

   4. **Paid Media Deep-Dive (Google Ads):**
      * Add a report utilizing `googleAdsKeyword`, `googleAdsAdNetworkType`, and `googleAdsCampaignName` to measure search-level performance.

   5. **Bulk Report Generation:**
      * Enhance the `--run-all-reports` feature to support date ranges like "Last 13 Months" for every report in one command.

   6. **Data API Quota Management:**
      * Add a check or display for "Property Quota" remaining, as aggressive reports can hit GA4 API limits.

   7. **Refactor output_manager.py:**
      * Consolidate HTML generation logic; currently, specialized reports (Trends, Top Channels) use custom saving blocks in run_report.py.

Current Status: The core engine is now robust, handling high-cardinality data and longitudinal trends. The project is ready for a Pull Request to the upstream repository.
